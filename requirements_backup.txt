# Core Scientific and Data Libraries (preserve original versions)
numpy==1.26.4
scipy==1.13.1
scikit-learn==1.5.0

# PyTorch (CPU)
torch==2.3.1
torchvision==0.18.1
torchaudio==2.3.1

# Hugging Face & sentence-transformers
transformers==4.42.3
safetensors==0.4.3
huggingface-hub==0.23.4
sentence-transformers==2.7.0

# Utilities
pillow>=10.4.0
tqdm==4.67.1
filelock==3.19.1
requests==2.32.5
packaging==25.0
pyyaml==6.0.3
regex==2025.9.18
python-dotenv>=1.0.1

# Vector store + PDF parsing
# NOTE: Removed specific versions for better compatibility, 
# you can re-pin if needed after successful installation.
pypdf
pymupdf
faiss-cpu
chromadb

# Groq and LangChain Ecosystem (LATEST and MODULAR)
# This set avoids the conflicts in the older 0.x.x versions.
# Installing these will pull in the main 'langchain-core' and 'langchain-community' 
# packages at versions compatible with each other.
groq
langchain
langchain-core
langchain-community
langchain-groq